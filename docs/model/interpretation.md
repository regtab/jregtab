# Table interpretation

Describe the table interpretation model (ITM) here: objects, operations,
semantics.

If you enable the arithmatex extension (see `mkdocs.yml`), formulas are written
in LaTeX:

$$
O_{\mathrm{join}} \;:\; \dots
$$
